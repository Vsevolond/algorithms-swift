//
//  main.swift
//  deliciousArithmetic
//
//  Created by Всеволод on 30.04.2023.
//

import Foundation

let k = Int(readLine()!)!

let n = max(Int(readLine()!)!, 30)

let costs = readLine()!.split(separator: " ").map { Int($0)! }

for i in (0..<n).reversed() {
    for j in (0..<i).reversed() {
        if 
    }
}
